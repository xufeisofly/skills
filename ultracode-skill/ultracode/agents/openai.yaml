interface:
  display_name: "Ultracode"
  short_description: "Dynamic workflows for Codex"
  default_prompt: "Use $ultracode to run a dynamic workflow for this coding task; prefer bounded native agents when useful."

policy:
  allow_implicit_invocation: false
